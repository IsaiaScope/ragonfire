# --- macOS AppleDouble (Crucial-4T is exFAT) ---
._*
.DS_Store
.Spotlight-V100
.Trashes
$RECYCLE.BIN/

# --- Big binary data (do NOT push) ---
# All runtime data lives under /data/: pgdata.ext4.img, ollama/, hf/, mineru/,
# input/, output/, working/, backups/. Fully gitignored — re-created on bootstrap
# (dirs) or downloaded on first use (models). Documentation for ollama/mineru
# lives in docs/{ollama,mineru}.md.
/data/

# RAG-Anything runtime (host-local)
rag-anything/.venv/
rag-anything/logs/
rag-anything/.env

# --- Python ---
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Editor / IDE ---
.vscode/
.idea/
*.swp
*.swo

# --- Logs / pid / tmp ---
*.log
*.pid
*.tmp
*.bak

# --- Node (if ever added) ---
node_modules/
.pnpm-store/
.iso/
